カテゴリ編集
<%= form_with model: @category, url: admin_category_path do |f| %>
    <label for="inputName">カテゴリ名</label>
      <%= f.text_field :name, autofocus: true, id:"inputName" %>
      <%= f.submit "変更を保存" %>
    <% end %>