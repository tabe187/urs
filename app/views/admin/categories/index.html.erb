カテゴリ一覧/作成
<%= form_with model: @category, url:admin_categories_path do |f| %>
    <label for="inputName">カテゴリ名</label>
      <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"カテゴリ名" %>
      <%= f.submit "新規登録" %>
    <% end %>
    --------------------------------------------<br>
    <% @categories.each do |category| %>
    <%= category.name %>
    <%= link_to "編集する", edit_admin_category_path(category.id) %><br>
    <% end %>
